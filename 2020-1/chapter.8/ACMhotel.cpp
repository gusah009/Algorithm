#include <iostream>
#define INF 987654321;

using namespace std;

int main() {  
    cin.tie(NULL);
    ios_base::sync_with_stdio(false);

    int T,H,W,N;

    cin >> T;
    for(int t = 0; t < T; t++) {
        cin >> H >> W >> N;
    }
    return 0;
}