#include <iostream>
#define INF 987654321;

using namespace std;

int main() {  
    cin.tie(NULL);
    ios_base::sync_with_stdio(false);

   char ascii;
   cin >> ascii;
   cout << (int)ascii;
    return 0;
}